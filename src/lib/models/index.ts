export interface ServerResponseModel {
  status: string;
  statusCode: number;
  message: string;
  timestamp: string;
}

export interface OfferingModel {
  image: string;
  title: string;
  description: string;
}